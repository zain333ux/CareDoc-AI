# Urdu document session implementation

Approved scope: English/Urdu at upload, Urdu dashboard labels and medical explanations, Urdu chat, original medication names/doses/dates/citations, Regular Nastaleeq. Landing and settings remain English.

1. Add `Language = Literal['english', 'urdu']` to upload and chat. Default to English for older clients. Persist language in the returned summary and localStorage document session.
2. Translate summary prose and instruction strings in one structured Groq call for Urdu. Mask medication identifiers, doses, dates, names and numeric literals before translation; reject changed/missing placeholders or list lengths. Retain original structured fields and expose translated display fields. Verify the translated summary against source. Return 502 with a retryable message on translation failure.
3. Generate grounded chat in English with original citations, translate only the answer for Urdu, preserving exact citations and numeric literals. Provide localized no-context refusal and explicit refusal flag.
4. Connect upload selector, remember its choice, add Urdu dashboard text/RTL, original-value bidi isolation, and localized chat errors. Load supplied Regular font locally for Arabic-script glyphs. Do not replace the visual design.
5. Test API language defaults/validation/propagation, translation token preservation and failure, original source retention, refusal language, frontend production build, and desktop/mobile Urdu rendering. Deploy tested files atomically to the connected GitHub main branch, then check both deployments.
